<div class="card h-full">
    <div class="card-header">
        <h3 class="card-title">
            Répartition de mes lectures
        </h3>
    </div>
    <div class="card-body flex flex-col gap-5">
        <figure id="graph" class="w-full"></figure>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/apexcharts"></script>
    <script>
        let category = [];
        {% for cate in categoryCount["category"] %}
            category.push("{{cate}}");
        {% endfor %}
        let count = [];
        {% for cate in categoryCount["count"] %}
            count.push({{cate}});
        {% endfor %}

        var options = {
            chart: {
                type: 'radar',
                width: '100%', 
                height: '450px' 

            },
            stroke: {
                show: true,
                curve: 'smooth',
            },
            markers: {
                size: 0 ,
            },
            series: [
                {
                name: "Book",
                data: count
                },
            ],
            labels: category
        }

        var chart = new ApexCharts(document.querySelector("#graph"), options);
        chart.render();

        function updateChart(){

            
            chart.updateSeries([{
                name: 'Book',
                data: data
            }])
        }

    </script>
</div>